<q-card-section class="row">
        <div>
          <img :ratio="1" style="width: 20px" src="@/assets/pasta.png" />
        </div>
        <q-expansion-item
          
          style="width: 80%"
          dense
          dense-toggle
          expand-separator
          label="Foto de motos"
        >
          <q-item style="width: 100px">
            <div v-for="item in moto" :key="item.id" class="row inline">
              <q-btn flat @click="icon = true">
                <q-card class="row inline">
                  <q-card-section>
                    <img :src="item.link" style="width: 100px" />
                    <p>{{ item.nome }}.{{ item.tipo }}</p>
                    <p style="color: #828782">tipo: {{ item.tipo }}</p>
                    <q-tooltip>
                      <p>descrição: {{ item.descricao }}</p>
                    </q-tooltip>
                  </q-card-section>
                </q-card>
              </q-btn>
              <q-dialog v-model="icon" :key="item.id">
                <q-card>
                  <q-card-section class="row items-center q-pb-none">
                    <div class="text-h6">{{ item.nome }}.{{ item.tipo }}</div>
                    <q-space />
                    <q-btn icon="close" flat round dense v-close-popup />
                  </q-card-section>

                  <q-card-section>
                    <img :src="item.link" />
                  </q-card-section>
                </q-card>
              </q-dialog>
            </div>
          </q-item>
        </q-expansion-item>
      </q-card-section>